module.exports = {
    purge: [],
    theme: {
        extend: {},
    },
    variants: {},
    plugins: [],
    corePlugins: {
        preflight: false // Disables the CSS normalization. This should be turned back on sooner than later.
    }
}
